services:
  vue-app:
    image: tsenturion/vue-performance-demo:latest
    build: .
    container_name: vue-performance-demo
    ports:
      - "8080:80"
    restart: unless-stopped

  vue-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: vue-performance-demo-dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
    command: npm run dev -- --host 0.0.0.0
